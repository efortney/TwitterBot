//=====PREPARED STATEMENTS=====//
//Statements are created as functions in order to be used in the exports module. 

module.exports = {
    /**
     * Inserts data into a sqlite3 database 
     * @param{tweet} object: a tweet object
     */
    insertTweet : function(object) {

    },

    /**
     * Inserts data into a sqlite3 database 
     * @param{user} object: a user object
     */
    insertUser : function(object) {

    }
}